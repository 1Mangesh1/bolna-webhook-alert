{
  "name": "bolna-webhook-alert",
  "version": "0.1.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "test": "node --test test/server.test.js",
    "test:local": "node --env-file=.env test/send-mock.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "^4.21.2"
  }
}
